# Validation queue — generated by post-commit hook, consumed by task validate.
# Transient state, not source of truth.
.validation-queue

# Python bytecode cache (gerado ao importar scripts/ci/*.py; não é source).
__pycache__/
*.pyc
